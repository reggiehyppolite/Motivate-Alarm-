<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
        Most Info.plist values are injected by XcodeGen from project.yml.
        This file exists so the build system has a stable path; XcodeGen will
        merge the `info.properties` block from project.yml into the generated
        plist at project-generation time.
    -->
</dict>
</plist>
